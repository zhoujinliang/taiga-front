div.wrapper
    tg-project-menu([project]="project|async", active="wiki")

    tg-wiki-nav([project]="project|async", [links]="links|async", *ngIf="linksVisible")
    section.main.wiki
        header
            h1
                tg-main-title([project]="project|async", sectionName="Wiki")

        // tg-wiki-wysiwyg([page]="page|async")

        tg-wiki-summary([page]="page|async", [project]="project|async", (delete)="onDelete($event)")

        tg-attachments-full([attachments]="pageAttachments|async")

        tg-wiki-history([historyEntries]="historyEntries|async")
