span {{ tag.get(0) }}
ng-container(*tgLoading="loadingRemoveTag")
    tg-svg.icon-close.e2e-delete-tag(
        *ngIf="canEdit",
        svg-icon="icon-close",
        svg-title-translate="COMMON.TAG.DELETE",
        (click)="delete.emit(tag)"
    )
